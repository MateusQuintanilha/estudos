# Colocando títulos em uma tabela
O Elemento HTML `<caption>` representa o título de uma tabela, ele sempre será o primeiro filho de um `<table>`.

```html
<table>
    <caption>Usuários</caption>

</table>
```
